# API reference

The reference is generated from NumPy-style docstrings via Sphinx
autosummary, the sklearn-ecosystem standard. It is sparse while the
package is pre-implementation and fills in automatically as public
modules land and acquire docstrings.

```{eval-rst}
.. autosummary::
   :toctree: generated
   :recursive:

   seq_sklearn
```

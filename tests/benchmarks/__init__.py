"""Unit tests for the comparative benchmark suite scaffold (Phase B0).

The default suite runs the cheap scaffold tests here (CPU-only, no
network, no real datasets). Heavyweight benchmark runs live under
the `benchmark` pytest mark (added in Phase B5+) and are NOT in PR
CI.
"""

"""Pydantic v2 configuration schemas for seq-sklearn.

Frozen models per A4. The :mod:`seq_sklearn.config._params_adapter` module
provides the BaseEstimator-adapter pattern that reconciles ``frozen=True``
with sklearn's mutation contract.
"""
